/**
 * Entry point for the Flight task. Gathers parameters and performs validation.
 */

// Currently stubs

import fli = require('./flight');

import tl = require('vsts-task-lib');


tl.debug('Flighting task');
fli.flightTask();
